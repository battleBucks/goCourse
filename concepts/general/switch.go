package main

import (
	"fmt"
)

func main() {
	favSport := "chess"
	switch favSport {
	case "soccer":
		fmt.Printf("favorite sport is %s\n", favSport)
	case "baseball":
		fmt.Printf("favorite sport is %s\n", favSport)
	case "football":
		fmt.Printf("favorite sport is %s\n", favSport)
	case "hockey":
		fmt.Printf("favorite sport is %s\n", favSport)
	case "chess":
		fmt.Printf("favorite sport is %s\n", favSport)
	case "basketball":
		fmt.Printf("favorite sport is %s\n", favSport)
	}
}
